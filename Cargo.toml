[package]
name = "smart-road"
version = "0.1.0"
edition = "2024"

[dependencies]
sdl2 = { version = "0.37", features = ["bundled"] }
rand = "0.8"
image = { version = "0.25", default-features = false, features = ["png"] }
